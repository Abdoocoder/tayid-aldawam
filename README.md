# تأييد الدوام - نظام إدارة الحضور الشهري 📊

نظام ذكي ومتكامل لإدارة حضور العمال الشهري مبني بتقنيات حديثة مع دعم كامل للغة العربية ونظام مصادقة آمن.

![Next.js](https://img.shields.io/badge/Next.js-16.0-black?style=flat-square&logo=next.js)
![TypeScript](https://img.shields.io/badge/TypeScript-5.0-blue?style=flat-square&logo=typescript)
![Supabase](https://img.shields.io/badge/Supabase-Enabled-green?style=flat-square&logo=supabase)
![Tailwind CSS](https://img.shields.io/badge/Tailwind-4.0-38bdf8?style=flat-square&logo=tailwind-css)

## ✨ المميزات

### 🔐 نظام مصادقة متكامل
- **Supabase Authentication**: نظام مصادقة آمن ومتكامل
- **تسجيل الدخول بالبريد الإلكتروني**: Email/Password authentication
- **OAuth Providers**: دعم تسجيل الدخول عبر Google و GitHub
- **إدارة الجلسات**: Session management مع استمرارية تسجيل الدخول
- **حماية المسارات**: Middleware لحماية صفحات التطبيق

### 👥 أربعة أدوار مختلفة
- **مدير النظام (ADMIN)**: صلاحيات كاملة لإدارة النظام
- **مراقب (SUPERVISOR)**: إدخال وتعديل حضور العمال
- **موارد بشرية (HR)**: إدارة المستخدمين والعمال والتقارير
- **قسم مالي (FINANCE)**: عرض الحسابات المالية والرواتب

### 💾 قاعدة بيانات قوية
- **Supabase PostgreSQL**: تخزين دائم وآمن للبيانات
- **Row Level Security (RLS)**: حماية البيانات على مستوى الصفوف
- **Real-time Subscriptions**: تحديثات فورية بين المستخدمين
- **Audit Logs**: سجل كامل لجميع التعديلات
- **نظام المناطق المتعددة**: دعم تعيين مشرف لعدة مناطق

### 🎨 واجهة مستخدم حديثة
- **تصميم متجاوب**: يعمل على جميع الأجهزة (موبايل، تابلت، ديسكتوب)
- **دعم RTL كامل**: واجهة عربية بالكامل مع دعم الاتجاه من اليمين لليسار
- **Tailwind CSS 4**: تصميم احترافي وعصري
- **مكونات قابلة لإعادة الاستخدام**: UI components محسّنة
- **تجربة مستخدم سلسة**: تفاعلات سريعة وسلسة

### ⚡ ميزات متقدمة
- **حسابات تلقائية**: احتساب الأيام والرواتب تلقائياً
- **تصدير CSV**: تصدير التقارير بصيغة CSV
- **بحث وتصفية**: بحث متقدم وتصفية حسب المنطقة
- **PWA Support**: دعم تطبيقات الويب التقدمية
- **إدارة المناطق**: إضافة وتعديل وحذف المناطق
- **إدارة المستخدمين**: CRUD كامل للمستخدمين والمشرفين

## 🚀 البدء السريع

### المتطلبات

- Node.js 18+ 
- npm أو yarn
- حساب Supabase (مجاني)

### التثبيت

1. **استنساخ المشروع**
```bash
git clone https://github.com/Abdoocoder/tayid-aldawam.git
cd tayid-aldawam
```

2. **تثبيت المكتبات**
```bash
npm install
```

3. **إعداد البيئة**

أنشئ ملف `.env.local` في المجلد الرئيسي:
```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

4. **إعداد قاعدة البيانات**

افتح مشروعك في Supabase واذهب إلى SQL Editor، ثم نفذ السكريبتات بالترتيب:

**أ. إعداد الجداول الأساسية:**
```sql
-- نفذ محتوى ملف supabase-setup.sql
```

**ب. إعداد نظام المصادقة:**
```sql
-- نفذ محتوى ملف supabase-auth-setup.sql
```

**ج. إعداد نظام المناطق المتعددة:**
```sql
-- نفذ محتوى ملف migration_areas.sql
```

5. **إعداد Supabase Authentication**

في لوحة تحكم Supabase:
- اذهب إلى **Authentication → Providers**
- فعّل **Email** provider
- (اختياري) فعّل **Google** و **GitHub** providers وأضف Client ID و Secret
- أضف Redirect URLs:
  - Development: `http://localhost:3000/auth/callback`
  - Production: `https://yourdomain.com/auth/callback`

6. **ترحيل البيانات الأولية** (اختياري)
```bash
npm run migrate
```

7. **تشغيل التطبيق**
```bash
npm run dev
```

افتح [http://localhost:3000](http://localhost:3000) في المتصفح.

## 📁 هيكل المشروع

```
Tayid-aldawam/
├── src/
│   ├── app/                          # Next.js App Router
│   │   ├── page.tsx                  # الصفحة الرئيسية (إعادة توجيه)
│   │   ├── login/                    # صفحة تسجيل الدخول
│   │   ├── register/                 # صفحة التسجيل
│   │   ├── auth/callback/            # OAuth callback
│   │   ├── dashboard/                # لوحة التحكم
│   │   │   ├── page.tsx             # Dashboard الرئيسي
│   │   │   └── entry/[id]/          # صفحة إدخال الحضور
│   │   └── layout.tsx                # Layout رئيسي
│   ├── components/
│   │   ├── ui/                       # مكونات UI قابلة لإعادة الاستخدام
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   ├── input.tsx
│   │   │   ├── select.tsx
│   │   │   ├── badge.tsx
│   │   │   └── month-year-picker.tsx
│   │   ├── views/                    # عروض حسب الدور
│   │   │   ├── AdminView.tsx        # لوحة المدير
│   │   │   ├── SupervisorView.tsx   # لوحة المراقب
│   │   │   ├── HRView.tsx           # لوحة الموارد البشرية
│   │   │   └── FinanceView.tsx      # لوحة القسم المالي
│   │   ├── layout/
│   │   │   └── Header.tsx           # رأس الصفحة
│   │   └── Providers.tsx             # Context Providers
│   ├── context/
│   │   ├── AuthContext.tsx           # إدارة المصادقة
│   │   └── AttendanceContext.tsx     # إدارة الحالة
│   ├── lib/
│   │   ├── supabase.ts               # Supabase APIs
│   │   ├── supabase-browser.ts       # Browser client
│   │   ├── supabase-server.ts        # Server client
│   │   ├── data-transformer.ts       # تحويل البيانات
│   │   └── utils.ts                  # Utility functions
│   └── data/
│       └── initialData.json          # بيانات أولية
├── scripts/
│   └── migrate-data.ts               # سكريبت الترحيل
├── public/
│   ├── manifest.json                 # PWA manifest
│   ├── icon-192.png                  # PWA icon
│   └── icon-512.png                  # PWA icon
├── supabase-setup.sql                # إعداد قاعدة البيانات
├── supabase-auth-setup.sql           # إعداد المصادقة
├── migration_areas.sql               # ترحيل نظام المناطق
└── package.json
```

## 👥 الأدوار والصلاحيات

### 1. مدير النظام (ADMIN) 🔑
صلاحيات كاملة لإدارة النظام:
- **إدارة المستخدمين**: إضافة، تعديل، حذف جميع المستخدمين
- **إدارة المشرفين**: تعيين المشرفين وإدارة مناطقهم
- **إدارة المناطق**: إضافة، تعديل، حذف المناطق
- **إدارة العمال**: CRUD كامل للعمال
- **سجل التدقيق**: عرض جميع التعديلات والتغييرات
- **الوصول الكامل**: الوصول لجميع البيانات والتقارير

### 2. المراقب (SUPERVISOR) 👷
إدارة حضور العمال في منطقته:
- **عرض العمال**: قائمة العمال في منطقته (أو جميع المناطق إذا كان مراقب عام)
- **إدخال الحضور**: إدخال وتعديل سجلات الحضور الشهرية
- **الأيام العادية**: تسجيل أيام العمل العادية
- **الأيام الإضافية**: تسجيل الإضافي العادي (0.5x) والإضافي في العطل (1.0x)
- **الحسابات التلقائية**: عرض الأيام المحتسبة تلقائياً

### 3. الموارد البشرية (HR) 📋
إدارة شاملة للموارد البشرية:
- **إدارة المشرفين**: إضافة، تعديل، حذف حسابات المشرفين
- **تعيين المناطق**: تعيين ونقل المناطق للمشرفين (دعم المناطق المتعددة)
- **إدارة العمال**: تعديل بيانات العمال ونقلهم بين المناطق
- **التقارير الشاملة**: عرض تقارير الحضور لجميع العمال
- **تصدير CSV**: تصدير التقارير بصيغة CSV
- **البحث والتصفية**: بحث متقدم وتصفية حسب المنطقة

### 4. القسم المالي (FINANCE) 💰
إدارة الحسابات والرواتب:
- **حسابات الرواتب**: عرض حسابات الرواتب لجميع العمال
- **الاحتساب التلقائي**: احتساب المستحقات تلقائياً بناءً على الحضور
- **البحث والتصفية**: بحث وتصفية حسب الاسم والمنطقة
- **تصدير CSV**: تصدير التقارير المالية
- **واجهة محسنة**: تصميم بألوان خضراء احترافية
- **عرض التفاصيل**: تفاصيل كاملة للأيام والحسابات

## 🔧 التقنيات المستخدمة

### Frontend
- **Next.js 16**: React framework مع App Router
- **React 19**: أحدث إصدار من React
- **TypeScript 5**: Type safety كامل
- **Tailwind CSS 4**: Utility-first CSS framework

### Backend & Database
- **Supabase**: Backend as a Service
  - PostgreSQL Database
  - Authentication (Email, Google, GitHub)
  - Real-time Subscriptions
  - Row Level Security (RLS)
  - Storage & APIs

### Libraries & Tools
- **Lucide React**: مكتبة الأيقونات
- **clsx & tailwind-merge**: إدارة CSS classes
- **Cairo Font**: خط عربي احترافي من Google Fonts
- **tsx**: TypeScript execution للسكريبتات

### Development Tools
- **ESLint**: Code linting
- **PostCSS**: CSS processing
- **dotenv**: Environment variables management

## 📊 قاعدة البيانات

### الجداول الرئيسية

#### 1. `workers` - جدول العمال
```sql
- id: TEXT (Primary Key)
- name: TEXT
- area_id: TEXT (Foreign Key → areas)
- base_salary: DECIMAL
- day_value: DECIMAL
- created_at, updated_at: TIMESTAMPTZ
```

#### 2. `attendance_records` - سجلات الحضور
```sql
- id: TEXT (Primary Key: workerId-month-year)
- worker_id: TEXT (Foreign Key → workers)
- month, year: INTEGER
- normal_days: INTEGER
- overtime_normal_days: INTEGER (0.5x)
- overtime_holiday_days: INTEGER (1.0x)
- total_calculated_days: DECIMAL (محسوبة تلقائياً)
- created_at, updated_at: TIMESTAMPTZ
```

#### 3. `users` - المستخدمين
```sql
- id: UUID (Primary Key)
- username: TEXT (Unique)
- name: TEXT
- role: TEXT (ADMIN, SUPERVISOR, HR, FINANCE)
- area_id: TEXT (للمشرفين - اختياري)
- auth_user_id: UUID (Foreign Key → auth.users)
- is_active: BOOLEAN
- created_at, updated_at: TIMESTAMPTZ
```

#### 4. `areas` - المناطق
```sql
- id: UUID (Primary Key)
- name: TEXT (Unique)
- created_at, updated_at: TIMESTAMPTZ
```

#### 5. `user_areas` - ربط المستخدمين بالمناطق
```sql
- user_id: UUID (Foreign Key → users)
- area_id: UUID (Foreign Key → areas)
- Primary Key (user_id, area_id)
```

#### 6. `audit_logs` - سجل التعديلات
```sql
- id: UUID (Primary Key)
- table_name: TEXT
- record_id: TEXT
- action: TEXT (INSERT, UPDATE, DELETE)
- old_data, new_data: JSONB
- changed_by: TEXT
- changed_at: TIMESTAMPTZ
```

### الحسابات التلقائية

```
الأيام المحتسبة = الأيام العادية + (الإضافي العادي × 0.5) + (الإضافي العطل × 1.0)
الراتب الشهري = الأيام المحتسبة × قيمة اليوم
```

**مثال:**
- أيام عادية: 26 يوم
- إضافي عادي: 4 أيام (= 2 يوم محتسب)
- إضافي عطل: 2 يوم (= 2 يوم محتسب)
- **المجموع**: 26 + 2 + 2 = **30 يوم محتسب**
- إذا كانت قيمة اليوم: 50 د.ل
- **الراتب**: 30 × 50 = **1,500 د.ل**

### Views (طرق العرض)

#### `payroll_view` - عرض الرواتب
يجمع بيانات العمال مع سجلات الحضور لحساب الرواتب

#### `monthly_summary` - الملخص الشهري
إحصائيات شاملة لكل شهر (عدد العمال، الأيام، إجمالي الرواتب)

### الأمان (Security)

- **Row Level Security (RLS)**: مفعّل على جميع الجداول
- **سياسات الأمان**: 
  - قراءة عامة للبيانات الأساسية
  - تعديل محدود حسب الدور والمنطقة
  - المشرفون يعدلون فقط في مناطقهم
  - HR و Finance و Admin لديهم صلاحيات أوسع
- **Triggers**: تحديث تلقائي للحقول والحسابات
- **Constraints**: قيود للتأكد من صحة البيانات

## 🛠️ السكريبتات المتاحة

```bash
# تشغيل التطبيق في وضع التطوير
npm run dev

# بناء التطبيق للإنتاج
npm run build

# تشغيل النسخة المبنية
npm run start

# فحص الكود (Linting)
npm run lint

# ترحيل البيانات الأولية إلى Supabase
npm run migrate
```

## 🔐 نظام المصادقة

### طرق تسجيل الدخول

1. **Email/Password**: تسجيل دخول تقليدي بالبريد الإلكتروني وكلمة المرور
2. **Google OAuth**: تسجيل دخول سريع عبر حساب Google
3. **GitHub OAuth**: تسجيل دخول عبر حساب GitHub

### تدفق المصادقة

1. المستخدم يسجل حساب جديد أو يسجل الدخول
2. Supabase Auth يتحقق من البيانات
3. عند النجاح، يتم إنشاء سجل في جدول `users` تلقائياً عبر Trigger
4. يتم ربط `auth.users.id` مع `users.auth_user_id`
5. يتم تحميل بيانات المستخدم وصلاحياته
6. إعادة التوجيه إلى Dashboard حسب الدور

### حماية المسارات

- جميع صفحات `/dashboard/*` محمية
- يتم التحقق من الجلسة عبر Middleware
- إعادة توجيه تلقائية للمستخدمين غير المصادق عليهم

## 🌟 الميزات المنفذة

- ✅ نظام مصادقة كامل (Supabase Auth)
- ✅ تسجيل الدخول عبر Email/Password
- ✅ تسجيل الدخول عبر Google و GitHub
- ✅ أربعة أدوار (ADMIN, SUPERVISOR, HR, FINANCE)
- ✅ نظام المناطق المتعددة
- ✅ إدارة المستخدمين والمشرفين (HR & Admin)
- ✅ إدارة العمال مع نقل المناطق
- ✅ سجل التدقيق (Audit Logs)
- ✅ تصدير التقارير CSV
- ✅ بحث وتصفية متقدمة
- ✅ Real-time synchronization
- ✅ Row Level Security (RLS)
- ✅ PWA Support
- ✅ تصميم متجاوب كامل
- ✅ دعم RTL كامل

## 📋 الميزات القادمة

- [ ] تصدير التقارير PDF
- [ ] رسوم بيانية وإحصائيات متقدمة
- [ ] إشعارات Push للمستخدمين
- [ ] وضع Dark Mode
- [ ] دعم اللغة الإنجليزية
- [ ] تطبيق موبايل (React Native)
- [ ] اختبارات تلقائية (Unit & E2E Tests)
- [ ] Dashboard للإحصائيات والتحليلات

## 🤝 المساهمة

المساهمات مرحب بها! يرجى:

1. Fork المشروع
2. إنشاء فرع للميزة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

## 📝 الترخيص

هذا المشروع مرخص تحت MIT License.

## 👨‍💻 المطور

تم تطويره بواسطة **Abdoocoder**

- GitHub: [@Abdoocoder](https://github.com/Abdoocoder)
- Repository: [tayid-aldawam](https://github.com/Abdoocoder/tayid-aldawam)

## 🆘 الدعم والمساعدة

إذا واجهت أي مشاكل:

1. تحقق من [Issues](https://github.com/Abdoocoder/tayid-aldawam/issues)
2. افتح Issue جديد مع وصف المشكلة
3. تأكد من تضمين:
   - وصف تفصيلي للمشكلة
   - خطوات إعادة إنتاج المشكلة
   - رسائل الخطأ (إن وجدت)
   - لقطات شاشة (إن أمكن)
   - نسخة المتصفح ونظام التشغيل

### مشاكل شائعة وحلولها

**مشكلة: خطأ في الاتصال بـ Supabase**
- تأكد من صحة `NEXT_PUBLIC_SUPABASE_URL` و `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- تحقق من تفعيل RLS Policies في Supabase

**مشكلة: فشل تسجيل الدخول**
- تأكد من تنفيذ `supabase-auth-setup.sql`
- تحقق من تفعيل Email provider في Supabase Dashboard

**مشكلة: OAuth لا يعمل**
- تأكد من إضافة Redirect URLs الصحيحة
- تحقق من صحة Client ID و Secret للـ OAuth provider

## 📸 لقطات الشاشة

### صفحة تسجيل الدخول
واجهة حديثة لتسجيل الدخول مع دعم Email/Password و OAuth (Google, GitHub)

### لوحة المدير (Admin)
- إدارة كاملة للمستخدمين والمشرفين
- إدارة المناطق
- عرض سجل التدقيق
- إحصائيات شاملة

### لوحة المراقب (Supervisor)
- عرض قائمة العمال في المنطقة
- إدخال وتعديل سجلات الحضور
- واجهة سهلة وسريعة

### لوحة الموارد البشرية (HR)
- إدارة المشرفين والعمال
- تقارير شاملة لجميع العمال
- تصدير CSV
- بحث وتصفية متقدمة

### لوحة القسم المالي (Finance)
- حسابات الرواتب والمستحقات
- تصميم بألوان خضراء احترافية
- بحث وتصفية حسب المنطقة
- تصدير التقارير المالية

---

## 🎯 الخلاصة

**تأييد الدوام** هو نظام متكامل وحديث لإدارة الحضور الشهري، مبني بأحدث التقنيات ومصمم خصيصاً للبيئة العربية. يوفر النظام:

- 🔒 **أمان عالي**: Supabase Auth + RLS
- ⚡ **أداء ممتاز**: Next.js 16 + React 19
- 🎨 **تصميم احترافي**: Tailwind CSS 4 + RTL
- 📊 **إدارة شاملة**: 4 أدوار مختلفة
- 🌐 **تحديثات فورية**: Real-time sync
- 📱 **PWA**: يعمل كتطبيق موبايل

⭐ إذا أعجبك المشروع، لا تنسى إعطائه نجمة على GitHub!
