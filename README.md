# نظام تأييد الدوام الذكي (Smart Attendance Support System)

نظام متكامل لإدارة الحضور والرواتب مصمم للشركات والمؤسسات، يتميز بتعدد الأدوار وسير عمل صارم للموافقة على البيانات المالية.

## 🚀 نظرة عامة

تم بناء هذا المشروع باستخدام أحدث التقنيات لضمان الأداء العالي وتجربة مستخدم ممتازة. يوفر النظام حماية قوية للبيانات من خلال التحكم في الوصول القائم على الأدوار (RBAC) وسياسات الأمان على مستوى الصف (RLS).

## 🛠 التقنيات المستخدمة

تم تطوير المشروع باستخدام مجموعة تقنية حديثة وقوية:

- **Framework**: [Next.js 16](https://nextjs.org/) (App Router)
- **Library**: [React 19](https://react.dev/)
- **Language**: [TypeScript](https://www.typescriptlang.org/)
- **Styling**: [Tailwind CSS 4](https://tailwindcss.com/)
- **Backend & Database**: [Supabase](https://supabase.com/) (PostgreSQL)
- **Authentication**: Supabase Auth
- **Icons**: Lucide React

## ⭐ المميزات الرئيسية

### 🔐 نظام الصلاحيات والأدوار
يحتوي النظام على 5 أدوار رئيسية، لكل منها واجهة وصلاحيات محددة:

1. **مشرف (Supervisor)**:
   - تسجيل الحضور اليومي للعمال في منطقته.
   - رفع التقارير للمراجعة الأولية.
   - *الواجهة*: `SupervisorView`

2. **مشرف عام (General Supervisor)**:
   - مراجعة تقارير المشرفين في قطاعاته.
   - اعتماد التقارير وتحويلها للموارد البشرية.
   - *الواجهة*: `GeneralSupervisorView`

3. **الموارد البشرية (HR)**:
   - إدارة بيانات المشرفين والعمال (إضافة، تعديل، نقل).
   - مراجعة تقارير الحضور (المرحلة الثالثة).
   - *الواجهة*: `HRView`

4. **المالية (Finance)**:
   - الاطلاع على التقارير المعتمدة من HR.
   - إجراء التعديلات المالية النهائية.
   - الاعتماد النهائي وصرف الرواتب.
   - *الواجهة*: `FinanceView`

5. **مدير النظام (Admin)**:
   - إدارة مستخدمي النظام وصلاحياتهم.
   - إعدادات النظام العامة.
   - *الواجهة*: `AdminView`

### 🔄 دورة حياة الاعتماد (Approval Workflow)
يمر سجل الحضور بـ 4 مراحل صارمة لضمان الدقة:
1. `PENDING_GS`: بانتظار المشرف العام.
2. `PENDING_HR`: بانتظار الموارد البشرية.
3. `PENDING_FINANCE`: بانتظار المالية.
4. `APPROVED`: معتمد نهائياً (مغلق للتعديل).

## 📂 هيكلة المشروع

```
src/
├── app/                 # صفحات التطبيق (Next.js App Router)
├── components/          # مكونات واجهة المستخدم
│   ├── ui/             # مكونات قابلة لإعادة الاستخدام (Buttons, Inputs...)
│   └── views/          # واجهات الأدوار (FinanceView, HRView, etc.)
├── context/            # إدارة الحالة (Auth, Attendance, Toast)
├── lib/                # دوال مساعدة والاتصال بقاعدة البيانات (Supabase)
└── styles/             # ملفات التنسيق العامة
```

## 🚀 البدء في العمل (Getting Started)

### المتطلبات
- Node.js 20+
- حساب Supabase

### التثبيت

1. استنسخ المستودع:
```bash
git clone <repository-url>
```

2. ثبت الاعتماديات:
```bash
npm install
```

3. قم بإعداد متغيرات البيئة `.env.local`:
```env
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

4. تشغيل خادم التطوير:
```bash
npm run dev
```

## 📝 الحقوق
© 2025 نظام تأييد الدوام الذكي - جميع الحقوق محفوظة
